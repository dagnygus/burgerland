import { DnngForDirective } from './dnng-for.directive';

describe('DnngForDirective', () => {
  it('should create an instance', () => {
    const directive = new DnngForDirective();
    expect(directive).toBeTruthy();
  });
});
