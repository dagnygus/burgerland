import { EmailAlreadyInUseValidatorDirective } from './email-already-in-use-validator.directive';

describe('EmailAlreadyInUseValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailAlreadyInUseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
